name: PR Quality Checks

on:
  pull_request:
    branches: [ main ]

jobs:
  testing:
    name: Pytest Suite
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Tests via Makefile
        run: make tests

